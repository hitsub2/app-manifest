---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: apiservices-seismic
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: apiservices-seismic
  policy:
    semver:
      range: 1.0.x

